$('#haiku').append("<%= j render partial: 'haiku', collection: @list_many_haiku %>");
<% if @list_many_haiku.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@list_many_haiku) %>');
<% else %>
  $('.pagination').remove();
<% end %>
